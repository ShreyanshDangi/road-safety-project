# Module 4 — Repair Verifier Dependencies

# Core
ultralytics>=8.0.0        # YOLOv8 (Module 1 model)
scikit-image>=0.21.0      # SSIM computation
opencv-python>=4.8.0      # Image processing
Pillow>=10.0.0            # EXIF GPS extraction
numpy>=1.24.0             # Array operations

# Database (integrated system)
psycopg2-binary>=2.9.0    # PostgreSQL driver — reads complaints table

# HTTP (integrated system)
requests>=2.31.0          # Downloads before-image from Cloudinary URL

# API
fastapi>=0.104.0          # REST API framework
uvicorn[standard]>=0.24.0 # ASGI server
python-multipart>=0.0.6   # File upload support

# Utilities
scikit-learn>=1.3.0       # (shared with Module 2/3, may already be installed)
